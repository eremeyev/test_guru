<%= content_tag :h1, "Tests:" %>
<%= link_to "Create new test", new_admin_test_path(id: @current_user.id) %>
<br/><br/>
<% if @tests.present? %>
  <table>
    <tr>
      <th>Num</th>
      <th>Text</th>
      <th>Questions qty</th>
      <th colspan="4">Actions</th>
    </tr>
    <%= render @tests %>
  </table>
<% else %>
  There is no tests created yet.
<% end %>
