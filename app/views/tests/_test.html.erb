<tr>
  <td>
    <%= test_counter + 1 %>
  </td>
  <td>
    <%= test.title %>
  </td>
  <td>
    <%= link_to test.questions.count, test_questions_path(test_id: test) %>
  </td>
  <td>
    <%= progress_message(test, current_user) %>
  </td>
  <td>
    <%= button_to "Start", start_test_path(test) if test.questions.present? %>
    <%= "No questions can't start test. Add questions." if test.questions.blank? %>
  </td>
</tr>
